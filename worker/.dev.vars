# Local-dev-only values for `wrangler dev`. Real values live in Worker secrets
# (`npx wrangler secret put <NAME>`) and are never committed.
AUTHOR_PASSPHRASE=local-dev-passphrase
IP_HASH_KEY=local-dev-ip-hash-key
# RESEND_API_KEY intentionally unset locally: the Worker skips email sends.
