# Later

Ideas for future features. One entry per feature; enough detail that it can be picked up cold.

## Annotations (inline comments on posts)

**Designed — see `specs/annotations.md`.** Readers comment on essays by highlighting text, Google-Docs style. All product decisions are made (margin-card placement interleaved with sidenotes, Cloudflare Worker + D1 backend, passphrase author mode, sticky "Comments: on/off" toggle, per-thread hide-as-collapse). The spec is implementation-ready; start there, not here.
