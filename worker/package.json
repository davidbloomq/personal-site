{
  "name": "comments-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute comments --local --file schema.sql",
    "db:remote": "wrangler d1 execute comments --remote --file schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
